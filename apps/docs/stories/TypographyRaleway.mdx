import { Meta, Typeset, Title } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Typography/Raleway" />

export const sizes = [12, 14, 16, 20, 24, 32, 40, 48];

export const weights = [
  [100, "Thin"],
  [200, "ExtraLight"],
  [300, "Light"],
  [400, "Regular"],
  [500, "Medium"],
  [600, "SemiBold"],
  [700, "Bold"],
  [800, "ExtraBold"],
  [900, "Black"],
];

export const sample = "The quick brown fox jumps over the lazy dog.";

export const ladder = (fontFamily) =>
  weights.map(([weight, name]) => (
    <div key={weight}>
      <h3>{`Font weight: ${name} (${weight})`}</h3>
      <Typeset
        fontSizes={sizes}
        fontWeight={weight}
        sampleText={sample}
        fontFamily={fontFamily}
      />
    </div>
  ));

<Title>Raleway — Titles / Display</Title>

Variable font covering the full **100–900** weight range.

{ladder("Raleway Variable, sans-serif")}
